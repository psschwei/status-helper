apiVersion: v1
kind: Service
metadata:
  name: status-assistant
  namespace: status-assistant
  labels:
    app.kubernetes.io/name: status-assistant
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: status-assistant
  ports:
    - name: http
      port: 80
      targetPort: http
